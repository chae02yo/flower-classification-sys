package com.example.backend.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface FlowerPredictFeedbackRepository extends JpaRepository<FlowerPredictFeedback, Long> {
}
